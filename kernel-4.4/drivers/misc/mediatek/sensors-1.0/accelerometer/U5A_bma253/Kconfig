config MTK_U5A_BMA2X2
	bool "BMA2X2 for MediaTek package"
	default y
config MTK_BMA2XX
	bool "bma2xx g-sensor for MediaTek package"
        default n
	help
	  bma2xx driver support some bosh g-sensor chip
	  such as bma220, bma222, etc .
	  now this driver is used for bma223.
